## Hello world!
Chceme umět používat C++ kompilátor (např. `g++`, `clang++`, `msvc`) a umět pracovat s `std::vector`. `std::vector` budeme používat pro dynamická pole, zatím do něj potřebujeme umět přidávat věci, zapisovat, procházet pomocí `:` a zjistit velikost skrze `.size()`.

Chceme vědět, jaký je rozdíl mezi `f(T item)` a `f(const T& item)`.
